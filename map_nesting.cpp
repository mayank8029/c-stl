#include<bits/stdc++.h>

using namespace std ; 

int main(){
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

map<pair<int,int> , int > m ;

pair<int , int > p1 , p2 ; 
p1 ={2,2};
p2 = {2,3};

cout<< (p1<p2);

// first we have to compare the first element of p1 and p2 whose vlae is greater will decide the greatness of p1 and p2 if both are equal then move to the next value


}